version: 2

sphinx:
  configuration: docs/conf.py

formats: all

python:
  version: "3.8"
  install:
    - requirements: requirements_docs.txt
    - method: pip
      path: .
      extra_requirements:
        - all

submodules:
  include: all